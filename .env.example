# Copy to .env and fill in. .env is gitignored.
PLAUD_TOKEN=your-bearer-token-here

# Optional. Where downloaded audio should land. Supports ~ and relative paths
# (relative resolves against the script dir). Defaults to ./audio next to sync.py.
# PLAUD_AUDIO_DIR=~/Music/Plaud
